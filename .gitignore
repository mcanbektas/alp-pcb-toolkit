node_modules
dist
# Prerender'ın sunucu paketi (`vite build --ssr`). Yalnız derleme sırasında,
# Node'da koşar; `dist/` gibi üretilmiş çıktıdır ve depoya girmez.
.prerender
.DS_Store
*.local

# api/ (.NET)
bin/
obj/
*.user
appsettings.*.json
!appsettings.json
!appsettings.Development.json.example
api/Alp.Api/App_Data/

# deploy/ (gizli değerler depoya girmez)
deploy/.env
deploy/*.env
